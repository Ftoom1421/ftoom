using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class spherspawner : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        //     if (Input.GetKeyDown(KeyCode.speeds))
        //     {
        //         spawnshpere():
        //     InokeRepeating("SpawnShpere", 0.0f, 1.0f);

        //     }
        //     if (Input.GetKeyUp(KeyCode.speeds))
        //     {
        //         CancelInvoke():
        //    }
    }
    void SpawnShpere()
    {
        // Instantiate(Shpere, transform.position, Quaternion.identity);
    }
}
